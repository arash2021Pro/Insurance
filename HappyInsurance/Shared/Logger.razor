
@using HappyInsurance.BlazorCoreModules.CoreComponents
@inherits HappyInsurance.BlazorCoreModules.CoreComponents.Logger

@code {
   
   protected override async Task OnAfterRenderAsync(bool firstRender)
   {
      if (firstRender)
      {
         await SetLogAsync();
      }
   }
}